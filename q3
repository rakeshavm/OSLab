if [ $# -ne 3 ]
	then
		echo not
		exit 1
fi

case "$2" in  
     "/") if [ $3 -eq 0 ]
    then 
echo devide by zero error
exit 1
else
    echo `expr $1 / $3`
fi
;;
     "*") echo `expr $1 \* $3`
;;
     "-") echo `expr $1 - $3`
;;
     "+") echo `expr $1 + $3`


esac
